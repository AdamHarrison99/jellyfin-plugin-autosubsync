<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <RootNamespace>MeasureCheck</RootNamespace>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>measurecheck</AssemblyName>
  </PropertyGroup>

  <!-- The real source, linked so the harness exercises shipping code and not a copy. -->
  <ItemGroup>
    <Compile Include="..\..\..\Subtitles\SubtitleOffsetProbe.cs" Link="SubtitleOffsetProbe.cs" />
  </ItemGroup>

</Project>
